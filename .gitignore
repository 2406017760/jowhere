node_modules/
dist/
.astro/
.npm-cache/
.env
.DS_Store

# Pagefind search index, regenerated by `npm run build` into public/ so that
# `astro dev` can serve search too.
public/pagefind/
.chrome-*/

# 评论服务端（独立的 Workers/D1 项目，单独推到自己的仓库）
twikoo-server/
